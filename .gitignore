# 1. 파이썬 가상환경 (이미지에 보이는 .venv 폴더 제외)
.venv/
venv/
ENV/

# 2. 파이썬 실행 시 생성되는 캐시 파일
__pycache__/
*.py[cod]
*$py.class

# 3. PyCharm 설정 파일 (사용 중인 IDE 설정)
.idea/

# 4. 운영체제에서 생성하는 임시 파일
.DS_Store
Thumbs.db

# 5. 환경 변수 설정 파일 (보안을 위해 깃에 올리면 안 됨)
.env